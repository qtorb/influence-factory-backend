import React from 'react'
import ReactDOM from 'react-dom/client'
import ControlTowerBridges from './ControlTower-Bridges'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ControlTowerBridges />
  </React.StrictMode>,
)